# IMPORTANT: Update these values with your actual secrets before applying!
# You can also create the secret using kubectl:
# kubectl create secret generic luma-secrets --from-literal=key=value -n dev
# kubectl create secret generic luma-secrets --from-literal=key=value -n prod

apiVersion: v1
kind: Secret
metadata:
  name: luma-secrets
  namespace: dev
type: Opaque
stringData:
  postgres-password: "luma_password"
  stripe-secret-key: "sk_test_your_stripe_secret_key"
  stripe-webhook-secret: "whsec_US13ucEwbvgY05aUhLee7JgQgSicTaPU"
  stripe-connect-webhook-secret: "whsec_1MkS8pty6XeoHg5r2rpEwMzQg0cgiC0w"
  cognito-client-secret: "10mne6n8e9asl4p9fmcc7gurrh9csjasdkvkv7bcc6139gl08mka"
---
apiVersion: v1
kind: Secret
metadata:
  name: luma-secrets
  namespace: prod
type: Opaque
stringData:
  postgres-password: "CHANGE_ME_TO_SECURE_PASSWORD"
  stripe-secret-key: "sk_live_YOUR_PRODUCTION_STRIPE_KEY"
  stripe-webhook-secret: "whsec_YOUR_PRODUCTION_WEBHOOK_SECRET"
  stripe-connect-webhook-secret: "whsec_YOUR_PRODUCTION_CONNECT_WEBHOOK_SECRET"
  cognito-client-secret: "YOUR_PRODUCTION_COGNITO_CLIENT_SECRET"